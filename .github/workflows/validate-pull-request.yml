name: Validate Student Outlook changes

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out proposed changes
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Reject article media uploads
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if git diff --name-only "$BASE_SHA" "$HEAD_SHA" | grep -q '^public/article-media/'; then
            echo "Image uploads are not enabled for Student Outlook articles."
            exit 1
          fi

      - name: Protect published articles
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
        run: node scripts/validate-published-changes.mjs "$BASE_SHA" "$HEAD_SHA" "$PR_AUTHOR"

      - name: Run ESLint
        run: npm run lint

      - name: Test writer authentication
        run: npm run test:auth

      - name: Test the publishing workflow
        run: npm run test:publishing

      - name: Check TypeScript
        run: npm run typecheck

      - name: Prepare draft articles for build validation
        run: node scripts/promote-submissions.mjs

      - name: Build the complete website
        run: npm run build
